1. Обучение модели
-- Убедитесь, что файлы .py находятся в том же каталоге, используйте файл "train.py"
-- В "class Config" измените путь к своему набору данных и параметры обучения
-- Запустите функцию main(), чтобы начать обучение
-- После завершения обучения веса модели, словарь и параметры конфигурации по умолчанию сохраняются в "./ ImageCaptioningModel.pth"

2. Вывод (инференс)
-- Убедитесь, что файлы .py находятся в том же каталоге, используйте файл "inference.py"
-- Запустите функцию main(), введите путь к изображению в консоли
-- Будет показано исходное изображение и его описание
